steps to create backend

1.generate package.json file
    npm init -y
2.create http server
    - install express module
        npm install express
    - import express module and create module


structure of a route
app.METHOD(path,request handler)


node install -g nodemon (for auto restart server)


post req and put req send the data as the body of the req object. get and delete do not support req object so these two req can send data through endpoint
