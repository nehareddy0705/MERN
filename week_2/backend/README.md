# backend

1. `npm init -y` then `npm install express`
2. `node server.js` — server runs on port 3000
3. Use `req.http` to send GET/POST/PUT requests

Route shape: `app.METHOD(path, handler)`. POST/PUT send data in the request body.
